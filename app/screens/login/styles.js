import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  loginContainer : {
    width : '100%',
    height : 60,
    alignItems : 'center'
  },
  registerContainer : {
    position : 'absolute',
    bottom : 20
  },
});

export default styles;